html
    head
        meta(charset="utf-8")
        meta(name="viewport" content="width=device-width, initial-scale=1")
        title= "Quizzer login"
        style
            include main.css
            include start.css
            include quiz.css
            include results.css
            include login.css
    body
        include topbar
        
        div(id="login-form")
            form(action="/login" method="POST")
                p(class="big-font")= "Login:"
                input(type="text" name="username" placeholder="username")
                div
                input(type="password" name="password" placeholder="password")
                div
                input(type="submit" value="Login" class="dark-button")